apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tictactoe-backend-chart.fullname" . }}-test-connection"
  labels:
    {{- include "tictactoe-backend-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tictactoe-backend-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
