apiVersion: v1
kind: Service
metadata:
  name: {{ include "tictactoe-backend-chart.fullname" . }}
  labels:
    {{- include "tictactoe-backend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tictactoe-backend-chart.selectorLabels" . | nindent 4 }}
