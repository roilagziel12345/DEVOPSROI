apiVersion: v1
kind: Service
metadata:
  name: tictactoe-frontend-service
  labels:
    app: tictactoe
    tier: frontend
spec:
  selector:
    app: tictactoe
    tier: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
