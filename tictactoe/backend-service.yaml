apiVersion: v1
kind: Service
metadata:
  name: tictactoe-backend-service
  labels:
    app: tictactoe
    tier: backend
spec:
  selector:
    app: tictactoe
    tier: backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
